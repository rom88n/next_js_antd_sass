const initialState = {
  loadingPage: false,
  data: {},
  dataDownloaded: false,
};

export const data = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
